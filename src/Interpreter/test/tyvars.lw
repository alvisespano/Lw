
namespace Test


let f (x : 'pippo) = x

let g (x : 'pippo) (y : 'baudo) =
    let f (z : 'pippo) = x, z
    in
        f y
        
let rec f g h x y = g (x : 'bau) + h (y : 'bau)
and e (x : 'bau) = x + 1

