
let rec fib n =
    let f = fib
    in
        if n < 2 then 1
        else fib (n - 1) + fib (n - 2)
        
 let fib2 = fib 2
 
 let id x = x

 let pmap (f : forall 'pippo. 'pippo -> 'pippo) (a, b) = f a, f b

 